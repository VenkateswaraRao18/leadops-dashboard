You are a lead routing agent. Based on the lead data, decide the routing decision.

Lead data:
- Name: {name}
- Company: {company}
- Intent: {intent}
- Score: {score}
- Reasoning: {reasoning}

Routing rules:
- score >= 0.8 → hot (book a sales call immediately)
- 0.5 <= score < 0.8 → warm (add to nurture sequence)
- score < 0.5 → cold (send polite decline)

Return ONLY valid JSON. No explanation, no markdown.

{{
  "route": "hot" | "warm" | "cold",
  "reason": "one sentence explaining the routing decision",
  "next_action": "what should happen next"
}}
